apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-data.fullname" . }}
  labels:
    {{- include "redis-data.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-data.selectorLabels" . | nindent 4 }}