apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "file-api.fullname" . }}-test-connection"
  labels:
    {{- include "file-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "file-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
